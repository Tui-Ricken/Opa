# URL do servidor Socket.IO.
# Em desenvolvimento local, aponte para localhost.
# Para conectar em outro computador na mesma rede, troque pelo IP da
# máquina que está rodando o /server (ex: http://192.168.0.10:3000).
VITE_SERVER_URL=http://localhost:3000
